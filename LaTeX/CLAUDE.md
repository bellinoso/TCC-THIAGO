# Instruções do projeto (TCC — LaTeX)

## Compilação

- **NUNCA compilar o documento.** Não executar `pdflatex`, `xelatex`, `lualatex`,
  `bibtex`, `biber`, `latexmk` nem qualquer ferramenta de build LaTeX. A compilação é feita manualmente
  pelo usuário.
